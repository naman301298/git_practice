console.log("Final branch code in this file okay");
//git push -u origin main


//Same command in long form

// Git suggested:

// git push --set-upstream origin final

// This is exactly the same as:

// git push -u origin final